&НаКлиенте
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    // Логотип
    Логотип = Элементы.Добавить("Логотип", "ПолеКартинки");
    Логотип.Ширина = 200;
    Логотип.Высота = 100;
    Логотип.Лево = 10;  // Вместо Новый Точка(10, 10)
    Логотип.Верх = 10;
    
    // Список продукции
    СписокПродукции = Элементы.Добавить("СписокПродукции", "ТаблицаФормы");
    СписокПродукции.Лево = 10;
    СписокПродукции.Верх = 120;
    СписокПродукции.Ширина = 800;
    СписокПродукции.Высота = 400;
    
    // Кнопка расчета
    КнопкаРасчета = Элементы.Добавить("КнопкаРасчета", "Кнопка");
    КнопкаРасчета.Лево = 820;
    КнопкаРасчета.Верх = 120;
    КнопкаРасчета.Заголовок = "Рассчитать время";
    КнопкаРасчета.ЦветФона = "#355CBD";
    КнопкаРасчета.ЦветТекста = "#FFFFFF";
    
КонецПроцедуры